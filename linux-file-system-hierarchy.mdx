---
type: notes
title: linux-file-system-hierarchy
description: understanding the linux's file system hierarchy
tags:
  - linux
  - file system
lastmod: 2023-08-14
published: true
---

## The hierarchy

- /
  - `bin` user binaries (executables: ls,mkdir)
    - `user/bin` user binaries in modern linux systems
  - `sbin` system binaries
  - `etc` configuration files
  - `dev` device files
  - `proc` process information
  - `var` variable files
  - `tmp` temporary files
  - `usr` user programs
  - `home` home directories (can have multiple users)
  - `boot` boot loader files
  - `lib` system libraries
  - `opt` optional applications
  - `mnt` mount directory
  - `media` removable devices
  - `srv` service data
